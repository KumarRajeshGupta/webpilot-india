# WebPilot India — v1

React + Vite + Tailwind marketing website.

## Run
npm install
npm run dev

## Build
npm run build
npm run preview

## Before launch
Edit `src/main.jsx` and replace `WHATSAPP_NUMBER` with the real WhatsApp number, including country code and without `+` or spaces. Replace demo portfolio items and any placeholder business details before publishing.
